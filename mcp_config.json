{
  "env": {
    "DATABASE_URI": "${DATABASE_URI}"
  },
  "mcpServers": {
    "echodb": {
      "command": "${uvcommand}", //path to uv executable
      "args": [
        "run",
        "--directory",
        "${projectpath}", //path to project
        "src/main.py"
      ],
      "env": {
        "DATABASE_URI": "${DATABASE_URI}"
      }
    }
  }
}
